import './bootstrap';
import './mobile-nav';
import '@fortawesome/fontawesome-free/js/all.min.js';

import 'aos/dist/aos.css';
import AOS from 'aos';

// Inicijalizacija AOS-a čim se fajl učita
AOS.init({
    once: true,
    duration: 1000,
});